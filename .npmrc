auto-install-peers=false
strict-peer-dependencies=false
# Some pnpm 10 installations enable strict build-script failures globally.
# The workspace allowlist in pnpm-workspace.yaml controls which trusted builds run.
strict-dep-builds=false
